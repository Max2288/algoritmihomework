def numberOfMatches(n: int) -> int:
    """ Сложность данной программы O(1).
    
    Args:
        n (int): the number of teams in a tournament.

    Returns:
        int: the number of matches played in the tournament until a winner is decided.
    """
    return n-1
